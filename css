/* Reset and Base Styles */
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

:root {
    /* Brand Colors */
    --brand-primary: #27479d;   /* Updated Primary Color */
    --brand-secondary: #00cc8e; /* Updated Secondary Color */
    --brand-light: #F4F6F6;
    --brand-gray: #7f8c8d;      /* Adjusted gray for better contrast on white */
    --brand-dark-gray: #5a6466; /* Adjusted dark gray */

    /* Spacing */
    --spacing-xs: 0.5rem;
    --spacing-sm: 1rem;
    --spacing-md: 1.5rem;
    --spacing-lg: 2rem;
    --spacing-xl: 3rem;
    --spacing-2xl: 4rem;
    --spacing-3xl: 5rem;

    /* Breakpoints */
    --bp-mobile: 480px;
    --bp-tablet: 768px;
    --bp-desktop: 1024px;
    --bp-large: 1200px;
}

/* Body background is set via HTML class now */
body {
    font-family: 'Inter', sans-serif;
    line-height: 1.6;
    color: #333; /* Changed text color for better contrast on white */
    /* background-color is now white, set via HTML class */
}

a {
    text-decoration: none;
    color: var(--brand-primary); /* Use primary color for links */
}

a:hover {
    text-decoration: underline;
}

img {
    max-width: 100%;
    height: auto;
    display: block;
}

ul {
    list-style: none;
}

.container {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--spacing-sm);
}

.text-center {
    text-align: center;
}

/* Utility Classes */
.gradient-text {
    /* Updated gradient to use new colors */
    background: linear-gradient(90deg, var(--brand-primary), var(--brand-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text; /* Standard property */
}

/* Buttons */
.button {
    display: inline-block;
    padding: var(--spacing-sm) var(--spacing-md);
    border: none;
    border-radius: 0.5rem; /* 8px */
    font-weight: 600;
    font-size: 1rem;
    text-align: center;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
}
.primary-button {
    background-color: var(--brand-primary); /* Use new primary color */
    color: white; /* White text on primary */
}
.primary-button:hover {
    background-color: #1a3370; /* Darker shade of primary */
    text-decoration: none;
    opacity: 0.9;
}
.secondary-button {
    background-color: var(--brand-secondary); /* Use new secondary color */
    color: white; /* White text on secondary */
    border: none; /* Remove border */
}
.secondary-button:hover {
    background-color: #00a875; /* Darker shade of secondary */
    text-decoration: none;
    opacity: 0.9;
}
.pricing-button {
    margin-top: auto; /* Push button to bottom in flex layout */
    width: 100%; /* Full width on card */
    text-align: center;
}

/* Header */
.site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    /* Updated background to be opaque white */
    background-color: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(0, 0, 0, 0.1); /* Changed border color */
}
.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--spacing-sm) 0;
}
.logo-container {
    display: flex;
    align-items: center;
}
.logo-placeholder {
    width: 48px;
    height: 48px;
    /* Use primary color for logo placeholder */
    background-color: var(--brand-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    color: white; /* White text on logo */
    margin-right: var(--spacing-xs);
}
.site-title {
    font-family: 'Poppins', sans-serif;
    font-size: 1.5rem; /* 24px */
    font-weight: 700;
    color: var(--brand-primary); /* Use primary color for title */
}
.main-navigation .nav-list {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
}
.main-navigation .nav-list a:not(.button) {
    color: var(--brand-dark-gray); /* Use dark gray for nav links */
    font-weight: 500;
}
.main-navigation .nav-list a:not(.button):hover {
    color: var(--brand-primary); /* Use primary color on hover */
    text-decoration: none;
}
.mobile-menu-button {
    display: none; /* Hidden by default on larger screens */
    flex-direction: column;
    justify-content: space-around;
    width: 30px;
    height: 30px;
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 0;
}
.hamburger-line {
    width: 100%;
    height: 3px;
    /* Use primary color for hamburger lines */
    background-color: var(--brand-primary);
    transition: all 0.3s linear;
    transform-origin: center;
    border-radius: 10px;
}
.mobile-menu {
    display: none; /* Hidden by default */
    padding: var(--spacing-md);
    /* Use white background for mobile menu */
    background-color: white;
    border-top: 1px solid rgba(0, 0, 0, 0.1); /* Changed border color */
}
.mobile-menu.active,
.mobile-menu:not(.hidden) {
    display: block;
}
.mobile-nav-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
    align-items: center;
}
.mobile-button {
    width: 100%;
    text-align: center;
}


/* Main Content Sections */
.section {
    padding: var(--spacing-2xl) 0;
}
.section-header {
    margin-bottom: var(--spacing-xl);
}
.section-heading {
    font-family: 'Poppins', sans-serif;
    font-size: 2rem; /* 32px */
    font-weight: 700;
    margin-bottom: var(--spacing-sm);
    color: var(--brand-primary); /* Use primary color for section headings */
}
.section-description {
    font-size: 1.125rem; /* 18px */
    color: var(--brand-gray); /* Use adjusted gray */
    max-width: 750px;
    margin: 0 auto var(--spacing-lg);
}

/* Hero Section */
.hero-section {
    padding: calc(var(--spacing-2xl) * 1.5) 0 var(--spacing-2xl);
    text-align: center;
    position: relative;
    overflow: hidden;
    /* Updated background to light gray on white background */
    background-color: var(--brand-light);
    /* Removed grid background */
}
.hero-content {
    position: relative;
    z-index: 2;
}
.hero-heading {
    font-family: 'Poppins', sans-serif;
    font-size: 2.5rem; /* 40px */
    font-weight: 800;
    line-height: 1.2;
    margin-bottom: var(--spacing-md);
    color: #333; /* Ensure heading text is dark */
}
@media (min-width: 768px) {
    .hero-heading {
        font-size: 3.5rem; /* 56px */
    }
}
.hero-description {
    font-size: 1.125rem; /* 18px */
    color: var(--brand-gray); /* Use adjusted gray */
    max-width: 750px;
    margin: 0 auto var(--spacing-lg);
}
.hero-buttons {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
    justify-content: center;
    align-items: center;
}
@media (min-width: 768px) {
    .hero-buttons {
        flex-direction: row;
        gap: var(--spacing-md);
    }
}
.arrow {
    display: inline-block;
    transition: transform 0.2s ease;
}
.secondary-button:hover .arrow {
    transform: translateX(3px);
}

/* Philosophy Section */
.philosophy-section {
    /* Removed dark background, now uses white */
    background-color: transparent;
}
.features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--spacing-lg);
}
.feature-card {
    /* Use white background */
    background-color: white;
    padding: var(--spacing-lg);
    border-radius: 1rem; /* 16px */
    /* Updated border color */
    border: 1px solid rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05); /* Subtle shadow */
}
.feature-card:hover {
    transform: translateY(-5px);
    /* Updated box shadow */
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
}
.feature-heading {
    font-family: 'Poppins', sans-serif;
    font-size: 1.5rem; /* 24px */
    font-weight: 600;
    margin-bottom: var(--spacing-sm);
    color: var(--brand-primary); /* Use primary color */
}
.feature-description {
    color: var(--brand-gray); /* Use adjusted gray */
}

/* Courses Section */
.courses-section {
    /* Default background is now white */
    background-color: transparent;
}
.course-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-2xl);
}
.course-item {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
    align-items: center;
}
@media (min-width: 992px) {
    .course-item {
        flex-direction: row;
        align-items: center;
    }
    .course-item-reverse {
        flex-direction: row-reverse;
    }
}
.course-content {
    flex: 1;
    min-width: 0; /* Fix for flexbox overflow */
}
.course-tag {
    display: inline-block;
    /* Use secondary color for tag background */
    background-color: rgba(0, 204, 142, 0.1); /* Light version of secondary */
    color: var(--brand-secondary); /* Use secondary color for text */
    font-weight: 600;
    padding: var(--spacing-xs) var(--spacing-sm);
    border-radius: 9999px; /* Full rounded */
    font-size: 0.875rem; /* 14px */
    margin-bottom: var(--spacing-sm);
}
.course-title {
    font-family: 'Poppins', sans-serif;
    font-size: 2rem; /* 32px */
    font-weight: 700;
    margin-bottom: var(--spacing-xs);
    color: #333; /* Dark text */
}
.course-price {
    font-family: 'Poppins', sans-serif;
    font-size: 1.5rem; /* 24px */
    font-weight: 600;
    color: var(--brand-primary); /* Use primary color */
    margin-bottom: var(--spacing-sm);
}
.course-description {
    color: var(--brand-gray); /* Use adjusted gray */
    margin-bottom: var(--spacing-md);
}
.course-features {
    color: var(--brand-gray); /* Use adjusted gray */
    margin-bottom: var(--spacing-md);
}
.course-features li {
    margin-bottom: var(--spacing-xs);
    position: relative;
    padding-left: 1.5em;
}
.course-features li::before {
    content: "\2713"; /* Checkmark */
    color: var(--brand-secondary); /* Use secondary color for checkmark */
    position: absolute;
    left: 0;
    top: 0;
}
.tracks-list {
    color: var(--brand-gray); /* Use adjusted gray */
    margin-top: var(--spacing-xs);
    padding-left: 1.5em;
}
.tracks-list li {
    margin-bottom: var(--spacing-xs);
    list-style-type: disc;
}
.course-image {
    flex: 1;
    min-width: 0; /* Fix for flexbox overflow */
    border-radius: 1rem; /* 16px */
    overflow: hidden;
    /* Updated border color */
    border: 1px solid rgba(0, 0, 0, 0.1);
    /* Use light background */
    background-color: var(--brand-light);
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 250px; /* Minimum height for placeholder */
}
.course-image img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Cover the container */
}

/* Ethics Section */
.ethics-section {
    /* Default background is now white */
    background-color: transparent;
    position: relative;
}
.ethics-content {
    /* Use light background */
    background-color: var(--brand-light);
    border-radius: 1rem; /* 16px */
    padding: var(--spacing-xl);
    /* Updated border color */
    border: 1px solid rgba(39, 71, 157, 0.3); /* Light version of primary */
    position: relative;
    overflow: hidden;
    text-align: center;
}
/* Simple pseudo-glow effect using secondary color */
.ethics-content::before,
.ethics-content::after {
    content: "";
    position: absolute;
    width: 160px;
    height: 160px;
    border-radius: 50%;
    /* Use secondary color for glow effect */
    background-color: rgba(0, 204, 142, 0.1);
    filter: blur(30px);
    z-index: -1;
}
.ethics-content::before {
    top: -80px;
    right: -80px;
}
.ethics-content::after {
    bottom: -80px;
    left: -80px;
}
.ethics-quote {
    font-family: 'Poppins', sans-serif;
    font-size: 1.75rem; /* 28px */
    font-weight: 600;
    font-style: italic;
    margin: var(--spacing-lg) 0;
    color: #333; /* Dark text */
}
.ethics-description {
    color: var(--brand-gray); /* Use adjusted gray */
    max-width: 750px;
    margin: 0 auto;
}

/* Pricing Section */
.pricing-section {
    /* Removed dark background, now uses white */
    background-color: transparent;
}
.pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-lg);
}
.pricing-card {
    /* Use white background */
    background-color: white;
    padding: var(--spacing-lg);
    border-radius: 1rem; /* 16px */
    /* Updated border color */
    border: 1px solid rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    position: relative; /* For popular tag positioning */
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05); /* Subtle shadow */
}
.pricing-card:hover {
    transform: translateY(-5px);
    /* Updated box shadow */
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
}
.pricing-card-popular {
    /* Use secondary color for popular border */
    border: 2px solid var(--brand-secondary);
    transform: scale(1.03);
}
.pricing-card-popular:hover {
    transform: scale(1.03) translateY(-5px); /* Maintain scale on hover */
}
.popular-tag {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translate(-50%, -50%);
    /* Use secondary color for popular tag */
    background-color: var(--brand-secondary);
    color: white; /* White text */
    font-weight: bold;
    padding: var(--spacing-xs) var(--spacing-md);
    border-radius: 9999px; /* Full rounded */
    font-size: 0.75rem; /* 12px */
    white-space: nowrap;
}
.pricing-title {
    font-family: 'Poppins', sans-serif;
    font-size: 1.5rem; /* 24px */
    font-weight: 600;
    margin-bottom: var(--spacing-xs);
    color: #333; /* Dark text */
}
/* Removed .pricing-subtitle as it's no longer used */
.pricing-amount {
    font-family: 'Poppins', sans-serif;
    font-size: 2.5rem; /* 40px */
    font-weight: 700;
    margin-bottom: var(--spacing-xs);
    color: var(--brand-primary); /* Use primary color */
}
.pricing-note {
    color: var(--brand-gray); /* Use adjusted gray */
    margin-bottom: var(--spacing-md);
}
.pricing-features {
    color: var(--brand-gray); /* Use adjusted gray */
    margin-bottom: var(--spacing-lg);
    flex-grow: 1; /* Push button down */
}
.pricing-features li {
    margin-bottom: var(--spacing-xs);
    position: relative;
    padding-left: 1.5em;
}
.pricing-features li::before {
    content: "\2713"; /* Checkmark */
    color: var(--brand-secondary); /* Use secondary color for checkmark */
    position: absolute;
    left: 0;
    top: 0;
}

/* Footer */
.site-footer {
    /* Use light background */
    background-color: var(--brand-light);
    /* Updated border color */
    border-top: 1px solid rgba(0, 0, 0, 0.1);
    padding-top: var(--spacing-xl);
}
.footer-content {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xl);
    margin-bottom: var(--spacing-lg);
}
@media (min-width: 768px) {
    .footer-content {
        flex-direction: row;
        justify-content: space-between;
    }
}
.footer-logo-info {
    flex: 1;
}
.footer-links {
    display: flex;
    gap: var(--spacing-xl);
}
.footer-column {
    flex: 1;
}
.footer-heading {
    font-weight: 600;
    margin-bottom: var(--spacing-sm);
    text-transform: uppercase;
    font-size: 0.875rem; /* 14px */
    letter-spacing: 0.05em;
    color: #333; /* Dark text */
}
.footer-list {
    color: var(--brand-gray); /* Use adjusted gray */
}
.footer-list a {
    color: var(--brand-gray); /* Use adjusted gray */
    display: block;
    margin-bottom: var(--spacing-xs);
}
.footer-list a:hover {
    color: var(--brand-primary); /* Use primary color on hover */
    text-decoration: none;
}
.footer-description {
    color: var(--brand-gray); /* Use adjusted gray */
    margin-top: var(--spacing-sm);
}
.footer-bottom {
    text-align: center;
    padding: var(--spacing-md) 0;
    /* Updated border color */
    border-top: 1px solid rgba(0, 0, 0, 0.1);
    color: var(--brand-gray); /* Use adjusted gray */
    font-size: 0.875rem; /* 14px */
}

/* Responsive Adjustments */
@media (max-width: 767px) {
    .main-navigation .nav-list {
        display: none;
    }
    .mobile-menu-button {
        display: flex;
    }
    .section-heading {
        font-size: 1.75rem; /* 28px */
    }
    .hero-heading {
        font-size: 2rem; /* 32px */
    }
    .course-title {
        font-size: 1.75rem; /* 28px */
    }
    .pricing-amount {
        font-size: 2rem; /* 32px */
    }
    .footer-links {
        width: 100%;
        justify-content: space-between;
    }
}
